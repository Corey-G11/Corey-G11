# Copy to .env (or just export these in your shell) before running.

# Required: your Anthropic API key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the port (default 3000).
# PORT=3000

# Optional: override the model (default claude-opus-4-8).
# POLYGLOT_MODEL=claude-opus-4-8
